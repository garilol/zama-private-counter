{
  "name": "zama-private-counter",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "deploy:zama": "hardhat run scripts/deploy.js --network zama"
  },
  "devDependencies": {
    "@fhevm/hardhat-plugin": "^0.8.0",
    "@nomicfoundation/hardhat-ethers": "^3.0.0",
    "hardhat": "^2.22.0",
    "ethers": "^6.10.0",
    "dotenv": "^16.4.0"
  }
}
